export type GetChannelsInformationIn = {
    /**
     * The ID of the broadcaster whose channel you want to get.
     * To specify more than one ID, include this parameter for each broadcaster you want to get.
     */
    broadcaster_id: string[];
};
