export type UserChatColor = {
    /**
     * An ID that uniquely identifies the user.
     */
    user_id: string;
    /**
     * The user’s login name.
     */
    user_login: string;
    /**
     * The user’s display name.
     */
    user_name: string;
    /**
     * The Hex color code that the user uses in chat for their name.
     */
    color: string;
}
